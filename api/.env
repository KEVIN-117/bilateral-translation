APP_NAME="Bilateral Translation AI API"
API_VERSION=1.0.0

# Hugging Face Registry Configuration
HF_REPO_ID="k3rb3r0/translation"
HF_FILENAME="model.h5"

# Fallback path if we want to force load a local model
# MODEL_PATH="model.h5"

# Prefijo con el que se sirven los videos del catálogo de señas
# Por defecto /videos, que apunta a client/public/videos/
# SIGNS_VIDEO_BASE_URL=/videos
